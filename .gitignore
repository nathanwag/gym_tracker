node_modules/
__pycache__/

# Config local do Claude Code — nao faz parte do projeto.
.claude/

# Saidas de build dos projetos nativos. As pastas android/ e ios/ em si devem
# ser commitadas: sao os projetos nativos que o Capacitor gera uma unica vez.
android/app/build/
android/build/
android/.gradle/
android/local.properties
ios/App/build/
ios/App/Pods/
ios/DerivedData/

.DS_Store
Thumbs.db
*.log

# Workspace de scratch do Superpowers (ledger, briefs, reports de review) —
# nunca faz parte do codigo, so existe durante a execucao de um plano.
.superpowers/

# Specs/plans gerados pelo skill Superpowers durante sessoes de trabalho —
# rascunho de processo, nao documentacao do produto.
docs/superpowers/
